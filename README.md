# -Directory-Synchronizer-
A utility tool that compares the contents of two directories and ensures they are identical. It automatically detects missing or outdated files and synchronizes them by copying newer or absent items from one directory to the other.
